# AI 기반 음악 디깅 앱/웹 — 기획서 (v0.2)

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 (가칭) | AI 음악 디깅 (Digging Assistant) |
| 한 줄 정의 | 내가 실제로 좋아하는 곡의 음향적·맥락적 특성을 분석해, 아직 모르는 인접한 곡·아티스트를 찾아주는 개인 발굴 도구 |
| 기존 프로젝트와의 관계 | [[spotify-notion-mcp]]와는 완전히 별개의 신규 프로젝트. 단, Last.fm 연동 등 기존 경험은 재사용 |
| 개발 순서 | 기획 확정 → 프로토타입(Python) → 정식 백엔드(FastAPI, Deep Me급 구조) |

## 2. 목적 / 비전

- **1차 목적 (개인용)**: 내 취향을 기반으로 신곡·신인 아티스트를 발굴하는 개인 도구.
- **2차 목적 (확장 가능성)**: 데이터와 로직이 검증되면 다른 사람도 쓸 수 있는 범용 추천 웹/앱으로 확장.
- 처음부터 다중 유저를 가정하고 설계하지 않는다 — 콜드 스타트 문제 때문에 초기엔 오버엔지니어링 리스크가 큼. **"1차 목적을 먼저 완성 → 검증되면 2차로 확장"** 순서를 지킨다.

## 3. 타겟 유저

| 우선순위 | 대상 | 니즈 |
|---|---|---|
| 1순위 | 나 자신 | 재즈/힙합/재즈힙합/하우스·IDM 등 이미 챙겨 듣는 장르 안에서, 평단 호평작 위주가 아닌 "아직 안 알려진" 인접곡을 찾고 싶음 |
| 2순위 (미래) | 나와 비슷한 디깅 성향의 리스너 | 스트리밍 알고리즘이 주는 뻔한 추천이 아닌, 설명 가능한(왜 추천됐는지 알 수 있는) 발굴 경험 |

## 4. 핵심 문제 정의

기존 스트리밍 추천(예: Spotify Discover Weekly)의 한계:
- 협업 필터링 기반이라 "이미 많이 들은 곡과 비슷한 대중적인 곡" 위주로 수렴하는 경향
- 왜 추천됐는지 설명이 없음 (블랙박스)
- 2024년 11월 Spotify의 Audio Features/Recommendations API 폐쇄로, 외부 개발자는 애초에 이 데이터 계층에 접근할 수 없어짐

→ **차별화 포인트**: 직접 확보한 음향 분석(Essentia) + 메타데이터(Last.fm/MusicBrainz)를 조합해, "왜 이 곡을 추천했는지"를 설명할 수 있는 콘텐츠 기반 발굴 로직을 만든다.

## 5. 핵심 기능 (MVP 기준)

디깅은 두 개의 축으로 나눈다: **① 음향/장르 기반 디깅**(곡이 "어떻게 들리는가")과 **② 관계 기반 디깅**(곡·아티스트가 "누구와 연결돼 있는가"). 둘은 서로 다른 발견 경험을 주므로 별도 기능으로 분리한다.

**① 음향/장르 기반**
1. **음원 분석 파이프라인**: 보유 음원 또는 메타데이터 기반으로 곡의 음향 특성(BPM, key, energy 등) + 장르/태그 추출
2. **유사곡 탐색**: 시드 곡 1개 입력 → 음향/태그 벡터 기준 코사인 유사도로 인접곡 랭킹
3. **"왜 추천됐는가" 설명**: 유사도에 기여한 상위 특성(예: "템포와 재즈 텍스처가 유사함")을 함께 표시

**② 관계 기반 (사람 중심 디깅)** — 곡 하나에서 사람·조직의 연결망을 타고 들어가는 탐색
4. **협업 그래프 탐색**: 시드 곡의 작곡가/프로듀서/엔지니어가 참여한 다른 곡·아티스트 찾기
5. **레이블 동료 탐색**: 같은 레이블 소속 다른 아티스트 찾기
6. **샘플 원곡 추적**: 시드 곡이 샘플링한 원곡(또는 원곡 아티스트) 역추적
7. **영향 관계 탐색**: 아티스트가 영향받았다고 알려진 이전 세대 아티스트로 거슬러 올라가기
8. **관계 경로 설명**: "A는 B가 프로듀싱한 곡" 같은 연결 경로를 그래프 형태로 함께 보여줌 (①의 "왜 추천됐는가"와 동일한 설명 가능성 원칙 적용)

**공통**
9. **피드백 기록**: 좋아요/스킵 기록 → 추후 협업 필터링·개인화 가중치에 사용할 데이터 축적

**③ 청취 이력 기반 초개인화** — 앱 내 피드백을 기다리지 않고, 이미 쌓인 실제 청취 이력으로 개인화를 앞당김
10. **청취 이력 동기화**: Spotify(최근 재생·상위 청취곡·실시간 재생), Apple Music(최근 재생, 보조), YouTube Music(비공식, 선택적) 연동
11. **"질림 스코어" 계산**: 재생 횟수·최근성을 기반으로 곡별 과다 청취 정도를 계산
12. **추천 하향/제외 필터**: ①·②의 추천 결과에서 질림 스코어가 높은 곡·같은 아티스트의 곡을 하향 가중치 또는 제외 — "맨날 듣던 곡 또 추천" 문제의 직접적인 해결책
13. **재발견 우대**: 예전엔 자주 들었지만 최근엔 안 들은 곡을 재발견 후보로 우대

**④ 결과 내보내기** — 디깅 결과를 실제로 듣는 곳까지 연결
14. **플레이리스트 생성**: ①·②·③ 어떤 축에서 나온 추천 결과든, 원하는 곡들을 골라 Spotify/Apple Music/YouTube Music 중 선택한 플랫폼에 새 플레이리스트로 생성
15. **플랫폼 곡 매핑 재사용**: 6번 데이터 전략의 크로스워크(ISRC/퍼지 매칭)를 반대 방향으로 활용해, canonical 트랙 → 각 플랫폼의 실제 곡 ID로 역매핑
16. **매칭 실패 투명 처리**: 특정 플랫폼 카탈로그에 없는 곡은 "이 플랫폼에서 찾을 수 없음"으로 정직하게 표시 — 비슷한 곡으로 임의 대체하지 않음 (관계 데이터 공백 처리 원칙과 동일)

### MVP 이후 (Phase 2 후보 기능)
- 피드백 데이터가 쌓이면 콘텐츠 기반 + 협업 필터링 하이브리드로 전환
- 웹 UI에서 플레이리스트 단위 디깅 (여러 곡을 시드로)
- 멀티 유저 지원, 계정/취향 프로필

## 6. 데이터 전략

| 데이터 | 소스 | 비고 |
|---|---|---|
| 음향 특성 (tempo, key, energy 등) | Essentia (C++/Python, 로컬 분석) | Spotify API 경로가 막혀서 직접 분석이 유일한 방법. CPU 환경에서도 동작 가능 |
| 장르/태그 | Last.fm API, MusicBrainz | 기존 [[spotify-notion-mcp]] 경험 재사용 |
| 원본 음원 확보 | 본인 라이브러리 우선 | 저작권 이슈로 스트리밍 미리듣기 스크래핑 등은 배제 |
| 프로듀서/작곡가/엔지니어 협업 관계 | MusicBrainz (Artist↔Recording relationship: producer, composer, engineer, mix 등) | 공식 API로 안정적 조회 가능. 관계형 데이터라 그래프 순회 쿼리로 처리 |
| 레이블 소속 관계 | MusicBrainz (Label↔Artist), Discogs API (레이블별 아티스트 로스터·크레딧) | Discogs는 특히 힙합/일렉트로닉 레이블 크레딧 데이터가 풍부한 편 |
| 샘플링 원곡 | MusicBrainz (Recording↔Recording "samples" 관계, 곡 단위로 특정 안 될 경우 Artist↔Recording "samples from artist" 관계) | 이 분야 최대 데이터베이스인 WhoSampled는 2025년 11월 Spotify에 인수됨 — 공식 공개 API가 없었고, 인수 이후 외부 개발자에게 열릴 가능성은 더 낮아짐. MusicBrainz는 커뮤니티 기여 기반이라 커버리지가 WhoSampled보다 적음(특히 언더그라운드 곡) — 데이터 공백을 감안해야 함 |
| 아티스트 간 영향 관계 | MusicBrainz (Artist↔Artist "influenced by") | 크라우드소싱이라 커버리지가 가장 희박한 축. 있으면 쓰고 없으면 공백으로 남기는 방식으로 설계 |
| 청취 이력 (최근 재생·상위 청취곡·실시간 재생) | Spotify Web API (OAuth) | 가장 안정적인 1차 소스. 2024년 폐쇄 대상(Audio Features 등)과는 별개로 살아있음 |
| 청취 이력 (보조) | Apple Music API (MusicKit) | Apple Developer Program 유료 가입 필요, 최근 재생 이력 범위가 짧음(약 30곡) — 보조 소스로만 사용 |
| 청취 이력 (선택) | ytmusicapi (비공식) | 활발히 유지보수되고 있으나 공식 API가 아님 — 개인 사용 범위에서만 고려, 서비스 확장 시 ToS 리스크 재검토 필요 |
| 플랫폼 간 곡 매핑 | ISRC 또는 (아티스트명+곡명) 퍼지 매칭 → MusicBrainz 레코딩 연결 | Spotify/Apple Music/YouTube Music의 곡 ID가 서로 달라서 별도 매핑 레이어 필요. 청취 이력 가져오기(읽기)와 플레이리스트 생성(쓰기) 양쪽에 동일한 매핑 레이어 재사용 |
| 플레이리스트 생성 (쓰기 권한) | Spotify Web API(playlist-modify 스코프), Apple Music API(library-modify, MusicKit), ytmusicapi(비공식) | Spotify는 공식 API로 안정적. Apple Music은 이미 상정된 Developer Program 비용 범위 내. YouTube Music은 기존 비공식 API 리스크와 동일 선상 |

### 6-1. 라벨링(태그 정규화) 전략

- **통일 태그 체계**: 처음부터 자체 분류를 새로 만들지 않고, **Discogs의 Genre → Style 2단계 계층(약 400개 style)**을 canonical 체계로 채택. 이미 6번의 데이터 소스로 쓰고 있고, 아래 음원 분석 대체 모델과도 같은 체계로 맞아떨어짐
- **1순위 — 메타데이터**: Last.fm/MusicBrainz/Discogs에서 얻은 태그를 크로스워크(대응표)를 통해 Discogs 체계로 정규화 (제공사마다 태그 기준이 달라서 필요한 단계, 수동 큐레이션으로 시작해 점진 보강)
- **2순위 — 음원 분석 대체**: 메타데이터가 부족한 곡은 Essentia의 사전학습 모델 **Discogs-EffNet**을 로컬로 실행 — 정확히 같은 400개 Discogs style에 대한 활성화 값을 직접 출력하므로 별도 매핑 없이 바로 canonical 태그로 사용 가능. CPU 환경에서도 곡당 수 초 내 처리 가능
- **다중 라벨 + 출처 보존**: 곡당 단일 장르가 아닌 상위 N개 태그 + 신뢰도 저장, 원본 제공사 태그도 함께 보존(크로스워크 개선 근거로 활용)
- **보조 축(백로그)**: Essentia 생태계의 danceability, 보컬/악기 구분, arousal-valence(무드) 분류기 — 장르와 별개로 추후 "무드 기반 디깅" 확장 시 고려

## 7. 추천 로직 로드맵

두 축은 별개의 탐색 방식이라 하나로 억지로 합치지 않고, 사용자가 "음향으로 디깅" / "사람으로 디깅" 중 선택하는 구조로 시작한다.

1. **Phase 1 (콘텐츠 기반, 음향/장르 축)**: 음향 벡터 + 태그 벡터 → 코사인 유사도 랭킹. 데이터 없이도 바로 시작 가능.
2. **Phase 1' (관계 기반, 사람 축)**: MusicBrainz/Discogs 관계 데이터로 그래프를 구축하고, 시드 곡에서 N단계(예: 프로듀서 → 그 프로듀서의 다른 곡 → 그 곡의 다른 협업자) 이내를 탐색하는 그래프 순회. 개인 규모에서는 별도 그래프 DB 없이 관계형 테이블 + 재귀 쿼리로 충분.
3. **Phase 2 (하이브리드)**: 피드백 데이터가 쌓이면 협업 필터링 요소 추가해 콜드 스타트 이후의 개인화 정교화. 두 축을 조합해 "음향은 다르지만 같은 프로듀서" 같은 교차 추천도 이 단계에서 고려.
4. 콘텐츠 기반 결과가 너무 "비슷한 곡만" 주는 필터 버블 위험 → 의도적으로 유사도 상위권이 아닌 "적당히 먼" 구간(예: 유사도 0.6~0.8)에서 후보를 뽑는 "디깅 존" 로직 고려.
5. **청취 이력 기반 필터 (교차 적용)**: ①·②에서 나온 추천 후보 전체에, 청취 이력으로 계산한 "질림 스코어"를 곱해서 재랭킹. 앱 내 피드백(9번 기능)을 기다리지 않고도 Phase 1 단계부터 바로 적용 가능 — 사실상 콜드 스타트 문제를 상당 부분 우회함.

## 8. 기술 스택

| 단계 | 스택 |
|---|---|
| Phase 1 (프로토타입) | Python, Essentia, Last.fm/MusicBrainz API, scikit-learn(코사인 유사도), Jupyter/CLI |
| Phase 2 (정식 백엔드) | FastAPI, PostgreSQL(+pgvector), 분석 워커/API 서버 분리 (Deep Me 구조 재사용) |

## 9. 제약 사항 / 리스크

- **Spotify API 제약**: 신규 앱은 Audio Features, Recommendations, Related Artists 접근 불가 (2024.11.27 이후) → 자체 음향 분석 파이프라인 필수
- **콜드 스타트**: 초기 피드백 데이터 부재로 협업 필터링 불가 → 콘텐츠 기반으로 시작
- **하드웨어 제약**: GPU 없는 개발 환경 → 무거운 딥러닝 임베딩 모델 로컬 학습/추론은 배제, Essentia 등 CPU 친화적 도구 우선
- **저작권**: 원본 음원은 본인 소유 라이브러리 범위 내에서만 분석
- **관계 데이터 공백**: 샘플링/영향 관계는 크라우드소싱 데이터라 언더그라운드·비영어권 아티스트일수록 커버리지가 낮음 → "데이터 없음"을 정직하게 표시하고, 없는 관계를 추측으로 채우지 않는다
- **플랫폼 간 곡 ID 불일치**: Spotify/Apple Music/YouTube Music의 곡 ID가 서로 달라 ISRC/퍼지 매칭 레이어가 별도로 필요
- **YouTube Music 비공식 API 리스크**: ytmusicapi는 구글 미승인 방식이라 개인 사용 범위를 넘어서면(예: 다른 사람도 쓰는 서비스로 확장) ToS 위반 소지 재검토 필요
- **Apple Music 비용**: MusicKit 사용에는 Apple Developer Program 유료 가입($99/년)이 필요
- **카탈로그 불일치**: 플랫폼마다 보유 카탈로그가 달라 특정 곡이 일부 플랫폼에서 검색되지 않을 수 있음 → 억지 매칭 대신 "찾을 수 없음"으로 표시
- **쓰기 권한 동의**: 플레이리스트 생성은 읽기보다 민감한 OAuth 권한(계정 콘텐츠 수정)이라 사용자 동의 흐름을 명확히 설계해야 함

## 10. 마일스톤 (초안)

| 단계 | 목표 |
|---|---|
| M1 | Essentia로 보유 음원 일부 분석 + 특성 벡터 DB 구축 |
| M2 | Last.fm/MusicBrainz/Discogs 메타데이터 파이프라인 연동 + Discogs 체계 기반 태그 크로스워크 구축 |
| M3 | 코사인 유사도 기반 유사곡 탐색 프로토타입 (CLI) |
| M4 | MusicBrainz/Discogs 관계 데이터(프로듀서·레이블·샘플·영향) 수집 및 그래프 테이블 설계 |
| M5 | 관계 기반(사람 축) 탐색 프로토타입 (CLI) |
| M6 | Spotify 청취 이력(최근 재생·상위 청취곡) 동기화 + 질림 스코어 계산 로직 |
| M7 | 피드백 기록 구조 설계 |
| M8 | 추천 결과를 Spotify/Apple Music/YouTube Music 플레이리스트로 내보내는 기능 구현 (플랫폼 곡 역매핑 포함) |
| M9 | FastAPI 기반 정식 백엔드로 이관 |

## 11. 성공 지표 (1차, 개인용 기준)

- 시드 곡을 넣었을 때, 이미 알고 있던 곡이 아닌 "새로운" 곡이 상위 랭킹에 얼마나 나오는가 (발굴률)
- 추천된 곡 중 실제로 좋아요를 누른 비율 (적중률)
- 추천 이유 설명이 실제 청취 경험과 일치한다고 느끼는 정도 (정성 평가, 본인 판단)

## 12. 운영 기획

### 12-1. 운영 목표
- **주 목적**: 개발자 포트폴리오. 데이터 엔지니어링(다중 소스 통합·태그 정규화) + ML/오디오 분석(Essentia) + 그래프 순회(관계 기반 디깅) + 백엔드 아키텍처(Deep Me급 FastAPI)를 아우르는, 면접에서 설명할 만한 프로젝트로 완성하는 것이 최우선
- **부차 목표**: 수익도 났으면 하는 희망은 있으나, 지금 단계의 설계 목표는 아님
- **포트폴리오 효과를 높이는 요소**: 실제 배포된 라이브 데모, 본인 실제 청취 데이터 기반 결과물, 기술적 의사결정 과정을 기록한 문서/블로그(예: Spotify API 제약, Discogs 체계 채택 이유 등 — 지금까지의 기획 논의 자체가 좋은 소재)

### 12-2. 타겟 유저층 (3번 섹션 재확인 + 구체화)
| 우선순위 | 대상 | 비고 |
|---|---|---|
| 1순위 | 나 자신 | 3번 섹션과 동일 |
| 2순위 (미래) | 레코드 컬렉터·DJ | 관계 기반 디깅(프로듀서/샘플/레이블)에 가장 민감하게 반응할 집단 |
| 2순위 (미래) | 스트리밍 알고리즘에 피로감 느낀 열성 리스너 | 청취 이력 기반 "질림 스코어" 기능이 직접 어필하는 집단 |

이런 성향의 시장 자체가 존재한다는 참고 지표: Discogs는 로그아웃 상태 사용자만으로도 월 850만 명이 방문하고 하루 310만 페이지를 조회함 — 우리가 그 규모를 목표로 하는 건 아니지만, "음악을 진지하게 파고드는 사람들"의 존재 자체는 검증된 시장.

### 12-3. 수익 모델 후보 (지금 구현 안 함, 범용 서비스 확장 시점에 검토)
| 후보 | 설명 | 참고 |
|---|---|---|
| 프리미엄/구독 | 기본 무료 + 심화 기능(무제한 관계 그래프 탐색, 심층 청취 리포트) 유료 | Discogs는 과거 연 12달러 유료 구독 기능이 있었으나 이후 폐지하고 전체 무료로 전환한 전례 — 니치 음악 데이터 서비스에서 단독 구독 모델은 리스크가 있었음 |
| 마켓플레이스 제휴 수수료 | 발굴한 곡의 바이닐/음반 구매 링크(Discogs, Bandcamp 등) 연결 후 제휴 수수료 | Discogs가 구독 대신 채택해 자리잡은 모델 |
| B2B/데이터 라이선싱 | 관계 그래프·취향 데이터를 레이블/A&R 쪽에 제공 | 장기적으로 [[ar-career-consideration]]과 연결될 수 있는 옵션 — 아이디어 단계 |
| 후원(도네이션) | Patreon류 | 니치 커뮤니티 도구에서 리스크 낮은 시작점 |

### 12-4. 지금 설계에 반영할 것
- 수익화 로직 자체는 구현하지 않되, **유저 계정/사용량 추적 인프라**는 Phase 2 백엔드 설계(8번 기술 스택)에 자연스럽게 포함시켜 나중에 유료 티어를 얹기 쉽게 해둔다
- 기능 목록(5번)에서 "무료로 남길 것 vs 나중에 유료화 후보"를 구분 표시만 해둔다 (예: 무제한 관계 그래프 탐색 깊이는 유료화 후보)

---
*다음 단계: 이 기획서 검토 후 확정되면 M1(Essentia 셋업 + 보유 음원 분석)부터 프로토타입 착수.*
